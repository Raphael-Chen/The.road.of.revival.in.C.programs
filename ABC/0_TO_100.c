#include <stdio.h>

// Output from 1 to 100 on screen
int main(void)
{
    int value = 0;

    while (value <= 100)
    {
        printf("%d\n", value);
        value++;
    }

    return 0;
}
