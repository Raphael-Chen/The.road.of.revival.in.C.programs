#include <stdio.h>
#include <math.h>
/*
       #include <complex.h>

       double cabs(double complex z);
       float cabsf(float complex z);
       long double cabsl(long double complex z);

       Link with -lm.
*/

/*
int main(void)
{
    struct complexes complex_number;    // outmoded, not C11

    complex_number.x = 10;
    complex_number.y = 5;

    printf("Absolute value of 10,5 is %f\n",
           cabs(complex_number));
}
*/

int main( void )
{

    return 0;
}